# Ignore all generated catalog files in this folder
# Keep this .gitignore so the folder can be present in git

!.gitignore

.DS_Store
# Cache and temp files
.workflow_cache.json
*.cache
*.tmp

# OS files
.DS_Store
Thumbs.db

# Optional: keep README if you have one